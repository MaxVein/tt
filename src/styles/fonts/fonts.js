import { createGlobalStyle } from "styled-components";

const FontStyles = createGlobalStyle`
body {
  font-family: 'Open Sans', sans-serif, 'DM Serif Display', serif;
}
`;

export default FontStyles;
